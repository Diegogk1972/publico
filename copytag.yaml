- hosts: all
  tasks:
  - name: deploy a simple template file
    ansible.builtin.copy:
      src: /srv/nacho.txt
      dest: /tmp/nacho.txt
      owner: diego
      group: diego
      mode: '0755'
    
    
